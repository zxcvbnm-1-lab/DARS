spring.application.name=backend

spring.datasource.url=${DARS_DB_URL}
spring.datasource.username=${DARS_DB_USERNAME}
spring.datasource.password=${DARS_DB_PASSWORD}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false